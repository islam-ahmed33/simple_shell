#include <stdio.h>

/**
 * main - calculates the sum of two numbers.
 *
 * Return: 0 on success.
 */

int main(void)
{
	int n1 = 1, n2 = 10, sum = 0;

	sum = n1 + n2;

	printf("%d is sum of this 2num", sum);

	return (0);
}
